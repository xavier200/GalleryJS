var chico = ["Hulas","the WERC","In-N-Out","madison bear","burgers & brew"];
var current
function shuffleGallery()
{
var Random = chico[Math.floor(Math.random() * chico.length-1)];
console.log(chico);
while (chico < 3) {
  chico.length += "  " + chico.length;
  chico.length++;
}
}
